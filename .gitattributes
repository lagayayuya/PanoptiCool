# The render goldens are GENERATED instruments (HTML snapshots, ~7 000 lines): they act — they go
# red on any deviation — but their content does not read like code. Marked `linguist-generated` so
# GitHub folds them in diffs and drops them from the language statistics; nothing changes locally,
# and a golden diff still has to be examined line by line when it moves (CLAUDE.md: any change of
# behavior in the core is proven by a zero-diff golden).
web/src/ui/v2/__snapshots__/** linguist-generated
