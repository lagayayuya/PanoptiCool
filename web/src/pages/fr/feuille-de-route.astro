---
// « Feuille de route » (« Feuille de route verticale » mockup) — where the project stands, and
// where a hand would help. Static build (ADR-0001/0002); the page is a single `client:only`
// island, like the home page, because it renders the mobile variant of the mockup and thus reads
// the viewport (`useIsMobile`).
import '@fontsource/jetbrains-mono/latin-400.css';
import '@fontsource/jetbrains-mono/latin-500.css';
import '@fontsource/jetbrains-mono/latin-600.css';
import '@fontsource/jetbrains-mono/latin-700.css';
import { DEFAULT_LOCALE, HTML_LANG, type Locale } from '../../i18n/locales';
import SiteHead from '../../ui/SiteHead.astro';
import { RoadmapPage } from '../../ui/v2/RoadmapPage';

// The language comes from the page's FOLDER, it is not written here: it is what makes
// `<html lang>` true by construction in each language tree — and `i18n/current.ts`,
// which reads this attribute to fabricate the islands' links, depends on it entirely.
const locale = (Astro.currentLocale ?? DEFAULT_LOCALE) as Locale;
---

<!doctype html>
<html lang={HTML_LANG[locale]}>
  <head>
    <SiteHead
      title="PanoptiCool — feuille de route"
      description="Ce qui est fait, ce qui arrive : les étapes de PanoptiCool, une plateforme à la fois."
      path="/feuille-de-route"
    />
  </head>
  <body>
    <RoadmapPage client:only="preact" />
    <style is:global>
      :root {
        color-scheme: dark;
        font-family: 'JetBrains Mono', ui-monospace, 'SF Mono', Menlo, monospace;
      }
      body {
        margin: 0;
        background: #070b18; /* NAVY.bgPage */
        color: #e9eefb; /* NAVY.textBright */
      }
      a {
        color: #2fd4f0;
      }
    </style>
  </body>
</html>
